[defaults]
# Every deploy is a brand-new server with a new host key, so there is nothing to pin.
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent

[ssh_connection]
pipelining = True
